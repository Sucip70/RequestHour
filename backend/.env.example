# Supabase → Project Settings → Database → Connection string (URI)
# Replace [YOUR-PASSWORD] with your database password (URL-encode special chars).
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.vemlykidgvzvsxqygyfu.supabase.co:5432/postgres

# Secret for AES-GCM audio tokens (use a long random string, e.g. openssl rand -hex 32)
GAME_SECRET=

# Optional HTTP port (default 8080)
# PORT=8080
